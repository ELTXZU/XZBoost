XZBoost 2.1
30.06.2024

- Added a welcome message saying "Welcome to XZBoost Menu (user)!".

- Completely removed Fun Games.

- Sorted ALOT of things.

- Removed cls's (clear screen) from windows tweaks so the user could see something.

- Removed unnessecary @echo off's.

- Made the warning menu better.

- Made the xzboost menu to be in the middle instead of at the top.

- Added a check for admin privileges section to the very start of the script.

- Added DISM.exe /Online /Cleanup-Image /CheckHealth & DISM.exe /Online /Cleanup-Image /ScanHealth to Fix Windows errors section.
